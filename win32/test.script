//注释为两个'/'，当前只支持这一种注释方法

void init()
{
	print("init.....");

}
void main()
{
	CTest test = new CTest;
	int nVal = test->Add(1,2);
	print(nVal);
	delete test;

	int t1 = GetMsTime();

	int sum = 0;
	int i = 1;
	while (i <= 100)
	{
		int j = 1;
		while (j <= 100)
		{
			sum = sum + i*j;
			j = j + 1;
		}
		i = i + 1;
	}

	int t2 = GetMsTime() - t1;

	print("Calculation time:" + t2);
}
